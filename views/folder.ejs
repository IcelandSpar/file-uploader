<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Folder</title>
</head>
<body>
<h1>Folder View</h1>
<%-include('partials/upload-form') %>
<% if(locals.files) { %>
  <ul>
    <% files.forEach((file, indx) => { %>
      <li>
        <a href="/download/<%= file.fileName %>">
          <p><%= file.originalName %></p>
        </a>
        <p><%= file.fileName %></p>
        <form action="/form/delete-file-verify/<%= file.id %>" method="GET"><button type="submit">Delete File <%= file.id %></button></form>
      </li>
    <% }) %>
  </ul>
<% } %>
</body>
</html>